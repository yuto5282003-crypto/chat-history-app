# SLOTY — 時間共有マーケット

時間をスロット単位で売買できるマーケットプレイスアプリ。
「すれ違い」で今ヒマな人と即マッチ。チケット制でシンプルな課金体験。

## デモモード（UI確認用）

DB・認証・Stripe の設定不要でUI/動線を確認できます。

### ローカル起動

```bash
cp .env.example .env.local
npm install
npm run dev
```

ブラウザで http://localhost:3000 を開く（スマホ幅で確認推奨）。

### Vercel デプロイ手順

1. GitHub にリポジトリを push（済み）
2. [vercel.com](https://vercel.com) にログイン → **Add New Project**
3. GitHub リポジトリを選択してインポート
4. **Environment Variables** に以下を追加：
   - `NEXT_PUBLIC_DEMO_MODE` = `1`
   - `OWNER_EMAIL` = `demo@sloty.app`
   - `ADMIN_PASSCODE` = `sloty2024`
   - `RETENTION_DAYS` = `180`
5. **Deploy** をクリック
6. デプロイ完了後、発行された URL をスマホで開いて確認

## Ownerが /admin に入るための設定

| 環境変数 | 説明 | デモ値 |
|---------|------|--------|
| `OWNER_EMAIL` | Owner判定用メールアドレス | `demo@sloty.app` |
| `ADMIN_PASSCODE` | 管理画面ログインパスコード | `sloty2024` |
| `RETENTION_DAYS` | KYC画像保管期限（日） | `180` |

管理画面ログイン手順（URL直打ちのみ、ユーザーUIからの導線なし）：
1. ブラウザで `https://<your-domain>/admin` を直接開く
2. パスコード入力（デモ: `sloty2024`）
3. 30分間のセッションが開始
4. 各タブ（本人確認 / ユーザー / 通報 / セッション / 会話ログ / チケット / 監査ログ）で管理
5. ※ ユーザー向けUI（設定画面等）に管理画面への導線は表示されません

## 主要パス一覧

| パス | 画面 | 内容 |
|------|------|------|
| `/` | スプラッシュ | 2秒のグラデーションスプラッシュ → /market |
| `/market` | マーケット | 時間から探す / 今から探す（スロット一覧） |
| `/market/sell` | スロット出品 | 日時・形式・カテゴリ・チケット枚数を入力して出品（1🎫） |
| `/market/slots/[id]` | スロット詳細 | チケット消費量・出品者情報・予約ボタン |
| `/nearby` | すれ違い | Leaflet地図・チェックイン・AI待ち合わせ提案 |
| `/pings` | ピン受信箱 | ピン承認/拒否・AI場所提案・初手メッセージ生成 |
| `/messages` | メッセージ | 予約紐付きチャットスレッド一覧 |
| `/calendar` | カレンダー | 月表示・フレンド予定オーバーレイ・共通空き時間・スワイプモーダル |
| `/square` | 広場 | 投稿一覧（いいね・依頼ボタン） |
| `/square/new` | 新規投稿 | テキスト・タグ・形式を選んで投稿（2🎫消費） |
| `/square/request/[id]` | 時間共有依頼 | 日時・形式・チケット上限を指定して依頼（5🎫消費） |
| `/verify` | 本人確認申請 | セルフィー・身分証・ライブネスチェックを提出 |
| `/friends` | フレンド一覧 | 権限設定・カレンダーリンク・非公開予定リンク |
| `/friends/invite` | フレンド招待 | QR/リンクで招待 |
| `/friends/calendar/[id]` | フレンドカレンダー | 週/月表示のFree/Busy |
| `/friends/events` | 非公開予定 | 予定CRUD（衝突チェック付き） |
| `/bookings` | 予約一覧 | 進行中/過去・チャット/通話/ビデオ開始 |
| `/requests/inbox` | 依頼受信箱 | 依頼の承認/拒否 |
| `/rooms/:roomId` | チャットルーム | テキストチャット・外部連絡先マスキング・延長・通報 |
| `/rooms/:roomId/call` | 音声通話 | タイマー・ミュート・作業モード |
| `/rooms/:roomId/video` | ビデオ通話 | カメラ切替・ミュート・タイマー |
| `/profile` | 設定 | Tinder風プロフィール・チケット購入/サブスク・eKYC申請 |
| `/admin` | 管理画面 | パスコード認証 → 7タブ管理 |

## 管理画面タブ構成

| タブ | 内容 |
|------|------|
| 本人確認 | AI→Owner最終確認・書類閲覧（ウォーターマーク付き・DL不可） |
| ユーザー | 検索（表記揺れ対応）・メール/ログイン方式表示・凍結/パスワード強制リセット |
| 通報 | 通報一覧・対象ルーム/ユーザー表示 |
| セッション | 成立予約・通話回数 |
| 会話ログ | ルーム選択→会話履歴（ページング・遡り無制限）・画像送信履歴（DL不可） |
| チケット | 台帳（入出金履歴） |
| 監査ログ | 全管理操作の記録（誰が・何を・いつ） |

## チケット価格

### 単発購入
| パック | 価格 |
|--------|------|
| 10🎫 | ¥480 |
| 30🎫 | ¥1,200 |
| 80🎫 | ¥2,980 |
| 200🎫 | ¥6,980 |

### サブスクリプション
| プラン | 月額 | 特典 |
|--------|------|------|
| Basic | ¥980 | 毎月40🎫・週末ブースト参加権 |
| Plus | ¥1,980 | 毎月100🎫・すれ違い軽優先・ピンCD短縮 |

### 消費
| アクション | コスト |
|-----------|--------|
| ピン送信 | 5🎫 |
| 広場投稿 | 2🎫 |
| スロット出品 | 1🎫 |
| ブースト(30分) | 10🎫 |
| チェックイン | 0🎫 |
| 通話/対面完了 | +1🎫（1日上限3回・同一相手24h1回） |

## デモで確認できる操作一覧

1. **起動画面** — 2秒のグラデーションスプラッシュ
2. **チケット購入** — /profile でパック購入（デモ：即付与）・サブスク加入（Basic/Plus）
3. **本人確認** — /verify でLv1(セルフィー)/Lv2(身分証+ライブネス)申請 → AI自動スコア → /admin/KYC で承認
4. **すれ違い（Leaflet地図）** — /nearby で「イマヒマON」→ 地図にマーカー → ピン送信（5🎫）
5. **メッセージ** — /messages でスレッド一覧 → /rooms/:id でチャット
6. **管理画面** — /admin でパスコード入力 → 7タブ管理
7. **チャット安全機能** — 外部連絡先自動マスキング・通報/ブロック・時間窓制限
8. **管理画面 Messages** — 通報紐付きチャット閲覧（理由必須）・Break Glass
9. **管理画面 Users** — BAN/解除・強制PWリセット（平文閲覧禁止）
10. **監査ログ** — /admin/Audit Logs で全管理操作を確認
11. **スロット出品→購入** — /market/sell で出品（1🎫）→ 詳細で予約 → /bookings で確認
12. **広場→依頼** — /square で投稿（2🎫）→ 依頼（5🎫）→ /requests/inbox で承認
13. **カレンダー** — フレンド予定オーバーレイ・共通空き時間・スワイプモーダル
14. **横スワイプナビ** — 下部タブ間を左右スワイプで移動

## BottomNav構成

| タブ | アイコン | パス |
|------|---------|------|
| マーケット | 🏪 | /market |
| すれ違い | 📡 | /nearby |
| メッセージ | 💬 | /messages |
| カレンダー | 📅 | /calendar |
| 広場 | 🏛️ | /square |
| 設定 | ⚙️ | /profile |

## 技術スタック

- **Next.js 15** (App Router)
- **React 19**
- **Tailwind CSS 3**
- **Leaflet + OpenStreetMap** — 地図表示（APIキー不要）
- **Prisma 6** + PostgreSQL（本番時）
- **NextAuth.js v5**（本番時）
- **Stripe**（本番時）

## 設計方針

- **チケット制**: UIに現金(¥)表記は一切なし。全てチケット🎫で統一
- **時間が主役**: カレンダー→空き時間→マッチが基本フロー
- **eKYC必須**: Lv0=閲覧のみ、Lv1=通話OK、Lv2=対面OK
- **安全設計**: 予約紐付きルーム・時間窓制限・外部連絡先マスキング・通報/ブロック
- **プライバシー**: 位置情報はチェックイン時のみ取得、マーカーにジッター付与、距離は範囲表示
- **管理者最小権限**: KYC画像/チャット閲覧は理由必須・監査ログ・DL禁止・ウォーターマーク
- **パスワード保護**: 平文閲覧不可・強制リセットのみ
- **AI提案層**: `src/lib/ai.ts` にルールベース実装。将来LLM差し替え可能
- **デモモード**: `src/lib/demo-store.ts` でlocalStorage完結。本番DB差し替え前提

## 将来の差し替えポイント

| 現在（デモ） | 本番 |
|------------|------|
| `src/lib/demo-store.ts` localStorage | Prisma + PostgreSQL |
| `src/lib/ai.ts` ルールベース | LLM API (Claude等) |
| LeafletMap コンポーネント | React Leaflet / Mapbox GL |
| `navigator.geolocation` 直接呼び出し | バックエンドジオハッシュ管理 |
| eKYC ダミースコアリング | 外部eKYC API (TRUSTDOCK等) |
| チャット localStorage | WebSocket + DB |
| 通話/ビデオ UIのみ | WebRTC (src/lib/rtc.ts) |
| チケット購入 即付与 | Stripe Checkout + Webhook |
| サブスク 即反映 | Stripe Billing + Webhook |
| Admin passcode (固定値) | NextAuth + RBAC |
| KYC画像メタのみ | S3 + KMS + 署名URL |
